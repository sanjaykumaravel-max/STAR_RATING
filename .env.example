# Copy this file to .env before running the local observability stack.
# Use a unique long password; never commit the real .env file.
GRAFANA_ADMIN_PASSWORD=replace-with-a-long-unique-password

# Optional host-port overrides for local development.
APP_PORT=8080
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
